# {{PROJECT}} ⇄ {{PEER}} 專案規則

> **本檔屬於本專案。bootstrap 永遠不會覆寫它。** 與 `PROTOCOL.md` 一起，**每一輪都讀**。
>
> 專案專屬的規則都寫在這裡。`PROTOCOL.md`、`PROTOCOL-modes.md`、`DESIGN-DECISIONS.md`
> 屬於 collab-bus，重跑 bootstrap 時會更新——寫進那三份的修改，下次升級時會凍結在舊版。
>
> **優先順序**：本檔 > `PROTOCOL.md`。但 **Trust anchor 與 participant 的安全規則只能加嚴、
> 不能放寬**。
>
> 修改本檔屬於協定變更：走 bus 讓對方同意後才實施。
>
> 下面各節是**引導**。用不到的節整節刪掉；需要的節，把 `<!-- -->` 裡的說明換成實際規則。
> **不要在本檔寫機密**（客戶名稱、個人資料、未公開的數值）——本檔多半會進版本控制。

## Participants

<!-- 本專案的 participant id。預設是 bootstrap 建立的這兩個；改名或增加時更新這裡。 -->

- Claude：`claude-primary`
- {{PEER}}：`{{PEER}}-primary`

## 本專案不適用範本的哪些假設

<!--
範本預設「這是 git 管理的程式專案」「reviewer 可以讀 diff」等等。本專案不符合的地方寫在這裡。
例：
- 根目錄不是 git 管理，改檔無法復原（只有同步軟體的版本歷史）。因此 {{PEER}} 預設不修改
  交付物，建議寫成訊息回丟；除非人類在該則訊息中明確要求實作。
- review 的範圍由每則訊息的 `refs` 決定：指向 `docs/**` 是文件 review，指向 `src/**` 是程式 review。
-->

## 角色分工與 review 模式

<!--
若本專案固定某種分工，寫在這裡（會覆寫 `PROTOCOL.md` 的「常見分工」）。
例：
- 發現問題的一方修正，另一方檢查（finder-fixes 為預設）。啟用條件仍依 `PROTOCOL.md`。
- 新功能由請求對象實作（預設 Claude），另一方檢查。
-->

## Review 閘門

<!--
送出 review 請求或修正結果前，**修正者必須跑、檢查者必須獨立重跑**的東西。
例：
- 該模組的 unit test，加上整合測試 `tests/e2e/smoke.sh`，兩者都要過。
- 結果（通過數、失敗項目）附在訊息裡。
-->

## 不可碰的路徑

<!--
例：
- `vendor/`、`archive/` 不修改。
- `legacy/` 只讀，重寫另案處理。
-->

## 專案自訂的訊息 type

<!--
`PROTOCOL.md` 的 type 不夠用時，在這裡定義。例：
- `fix-request`：修正請求、退回
- `fix-result`：修正完成報告（修正內容、閘門結果、希望檢查的地方）
-->

## 機密與匿名化

<!--
例：
- 客戶與合作方的名稱一律寫成代稱，不寫本名。
- 提交前以 `scripts/anon_scan.sh` 掃描，輸出為空才算通過。
- 修正者遵守、檢查者掃描。
-->

## 版本控制與同步

<!--
例：
- git 管理的範圍、分支規則、誰決定 merge。
- 與雲端同步資料夾並用時，哪些檔案由哪一方同步（同一個檔案不要兩邊同時同步）。
-->
